# @package _global_
#
# Main all-view MoT training on RoboCasa T-shape. Equivalent to the legacy
# configs/robocasa_all_tshape_mot.py. All hyperparameters come from train.yaml;
# this task only selects the data/model groups and names the experiment.

defaults:
  - override /data: robocasa_all_tshape
  - override /model: mot
  - _self_

exp_name: robocasa_all_tshape_mot
